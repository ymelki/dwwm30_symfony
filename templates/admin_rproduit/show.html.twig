{% extends 'base.html.twig' %}

{% block title %}Rproduit{% endblock %}

{% block body %}
    <h1>Rproduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rproduit.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ rproduit.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ rproduit.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ rproduit.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_rproduit_index') }}">back to list</a>

    <a href="{{ path('app_admin_rproduit_edit', {'id': rproduit.id}) }}">edit</a>

    {{ include('admin_rproduit/_delete_form.html.twig') }}
{% endblock %}
